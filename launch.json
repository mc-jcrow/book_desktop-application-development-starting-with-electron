{
    "version": "0.0.1",
    "configurations": [
        {
            "name": "Debug Main Process",
            "type": "node",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "windows": {
                "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron"
            },
            "args": ["."],
            "outputCapture": "std"
        }
    ]
}